apiVersion: v1
kind: Service
metadata:
  name: hello-service
  labels:
    app: hello-service
spec:
  selector:
    app: hello-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
