apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels:
    app: products-service
spec:
  selector:
    app: products-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
